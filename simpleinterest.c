#include<stdio.h>

int main()
{
    int principal=100,rate=4,time=12;
    int simpleinterest=(principal*rate*time)/100;
    printf("the value of simple interest is %d\n",simpleinterest);
    return 0;
}